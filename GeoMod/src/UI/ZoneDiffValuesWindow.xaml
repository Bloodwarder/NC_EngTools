<Window x:Class="GeoMod.UI.ZoneDiffValuesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GeoMod.UI"
        mc:Ignorable="d"
        Title="Выбор зон" Height="450" Width="400" MinWidth="300"
        Name="zoneDiffValuesWindow" Loaded="zoneDiffValuesWindow_Loaded"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <ScrollViewer Name="svZoneDiffValues" Grid.Row="0" Margin="5" PreviewMouseWheel="svZoneDiffValues_PreviewMouseWheel" >
            <DataGrid Name="dgZoneDiffValues" AutoGenerateColumns="False" ItemsSource="{Binding Zones}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Слой зон" Width="*" Binding="{Binding Layer}"/>
                    <DataGridTextColumn Header="Величина" Width="100" Binding="{Binding Value}"/>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
        <Button Name="bOk" Grid.Row="1" Width="150" Height="35" Content="OK" Click="bOk_Click"/>
    </Grid>
</Window>
